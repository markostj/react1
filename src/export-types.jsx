import React from 'react';

// Named component
export const Header = () => <header />;

// Named Element
export const Footer = <footer />;

// Default component
export default () => <span>abcd</span>;
